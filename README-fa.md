# GoNix

*[English](README.md)*

پیش نمایش ویدیویی:
<p align="center">

https://github.com/user-attachments/assets/45f5a47a-ebe0-4b5c-a6c1-e2c971aaa8d5

</p>

**GoNix** یعنی **Go** + **Nginx** — یک **رابط کاربری متنی (TUI)** پیشرفته و تعاملی برای
**مدیریت Nginx** روی لینوکس که با Go نوشته شده.

## فهرست مطالب

- [نصب](#نصب)
  - [معماری‌های پشتیبانی‌شده](#معماریهای-پشتیبانی‌شده)
  - [چه چیزی و در کجا نصب می‌شود](#چه-چیزی-و-در-کجا-نصب-میشود)
  - [یا نصب از سورس](#یا-نصب-از-سورس)
  - [نحوه‌ی ساخت ریلیزها](#نحوهی-ساخت-ریلیزها)
- [امکانات](#امکانات)
- [پیش‌نیازها](#پیشنیازها)
- [ساخت از سورس](#ساخت-از-سورس)
  - [نصب از سورس با `make install`](#نصب-از-سورس-با-make-install)
  - [ساخت توسعه (development build)](#ساخت-توسعه-development-build)
  - [اجرا با `go run` (توسعه)](#اجرا-با-go-run-توسعه)
  - [ساخت نسخه‌ی نهایی (production)](#ساخت-نسخهی-نهایی-production)
  - [کراس-کامپایل](#کراس-کامپایل)
  - [Makefile](#makefile)
- [دستورات توسعه](#دستورات-توسعه)
- [ساختار پروژه](#ساختار-پروژه)
- [پیکربندی Nginx](#پیکربندی-nginx)
- [گواهی‌های SSL](#گواهیهای-ssl)
  - [دریافت گواهی Wildcard (Certbot)](#دریافت-گواهی-wildcard-certbot)
    - [تمدید یک گواهی](#تمدید-یک-گواهی)
- [Access List‌ها](#access-listها)
- [لاگ‌ها](#لاگها)
- [پشتیبان‌گیری (Backups)](#پشتیبانگیری-backups)
- [توسعه](#توسعه)
- [امکانات آینده](#امکانات-آینده)

ایده‌ی این پروژه از [Nginx Proxy Manager](https://nginxproxymanager.com/) گرفته شده، ولی مسیر
متفاوتی رفته: به‌جای اینکه Nginx را پشت یک دیتابیس و یک فرمت پیکربندی اختصاصی قایم کند، GoNix
یک لایه‌ی نازک و ایمن **روی** همان فایل‌های پیکربندی واقعی و قابل‌ویرایش Nginx می‌نشیند. یعنی
همیشه می‌تونید فایل `/etc/nginx/sites-available/example.com` را با یک ویرایشگر متن باز کنید و
سینتکس استاندارد و ساده‌ی Nginx را ببینید — هیچ‌چیز مبهم یا اختصاصی در کار نیست.

باینری کامپایل‌شده هم اسمش `gonix` است.

## نصب

آخرین نسخه‌ی GoNix را با یک دستور نصب کنید:

```bash
curl -fsSL https://raw.githubusercontent.com/mrmmg/gonix/main/install.sh | bash
```

همین‌قدر کافیه. نصب‌کننده خودش معماری سیستم شما را تشخیص می‌دهد، آخرین ریلیز را دانلود
می‌کند، GoNix را نصب می‌کند و پیکربندی پیش‌فرض را هم آماده می‌کند. بعد از تمام‌شدن کار، کافیه
اجرا کنید:

```bash
sudo gonix
```

اگر می‌خواهید به‌جای آخرین نسخه، یک نسخه‌ی مشخص نصب کنید:

```bash
curl -fsSL https://raw.githubusercontent.com/mrmmg/gonix/main/install.sh | bash -s -- v1.2.3
```

اجرای دوباره‌ی نصب‌کننده — با یا بدون آرگومان نسخه — مثل یک **بروزرسانی** روی نصب فعلی عمل
می‌کند: باینری `gonix` را با نسخه‌ی درخواستی جایگزین می‌کند و می‌گوید قبلاً چه نسخه‌ای نصب بوده،
اما هیچ‌وقت دست به پیکربندی موجود شما در `/etc/gonix` نمی‌زند.

اگر از قبل دسترسی `sudo` کش‌شده ندارید، نصب‌کننده هر وقت لازم شد در `/opt`، `/etc` یا
`/usr/local/bin` بنویسد، رمز عبورتان را می‌پرسد.

### معماری‌های پشتیبانی‌شده

| سیستم‌عامل | معماری | فایل ریلیز              |
|------------|--------|---------------------------|
| Linux      | amd64  | `gonix-linux-amd64`      |
| Linux      | arm64  | `gonix-linux-arm64`      |

هر ترکیب سیستم‌عامل/معماری دیگری با یک خطای واضح رد می‌شود، به‌جای اینکه نصب‌کننده چیزی دانلود
کند که اصلاً روی سیستم شما اجرا نمی‌شود.

### چه چیزی و در کجا نصب می‌شود

| مسیر                            | کاربرد                                                          |
|----------------------------------|-------------------------------------------------------------------|
| `/opt/gonix/bin/gonix`           | باینری نصب‌شده                                                     |
| `/opt/gonix/VERSION`             | نسخه‌ای که الان نصب است                                            |
| `/usr/local/bin/gonix`           | symlink به باینری بالا، تا `gonix` از هر جای `$PATH` در دسترس باشد |
| `/etc/gonix/gonix.yaml`          | فایل پیکربندی (فقط یک‌بار ساخته می‌شود، هرگز overwrite نمی‌شود)     |
| `/etc/gonix/backups/`            | پشتیبان‌های خودکار پیکربندی                                        |
| `/etc/gonix/accesslists/`        | فایل‌های Access List برای HTTP Basic Auth                          |
| `/var/log/gonix/audit.log`       | لاگ حسابرسی (audit log)                                            |
| `/etc/logrotate.d/gonix`         | سیاست چرخش لاگ (اگر `logrotate` نصب باشد)                          |

### یا نصب از سورس

اگر ترجیح می‌دهید خودتان محلی بسازیدش (بدون دانلود ریلیز از GitHub)، بخش «ساخت از سورس» را در
ادامه‌ی همین سند بخوانید — دستور `make install` باینری را با `go build` می‌سازد و آن را دقیقاً
در همان ساختار `/opt/gonix` که بالا توضیح دادیم نصب می‌کند.

### نحوه‌ی ساخت ریلیزها

با push کردن یک تگ نسخه (`vX.Y.Z`، مثلاً `v1.2.3`) به این مخزن، ورک‌فلوی
[`Release` در GitHub Actions](.github/workflows/release.yml) اجرا می‌شود، باینری‌های
`linux/amd64` و `linux/arm64` را می‌سازد و — فقط اگر هر دو build موفق شوند — یک GitHub Release
دقیقاً برای همان تگ منتشر می‌کند که شامل باینری‌ها، یک `checksums.txt` و پیکربندی پیش‌فرض است.
هیچ بخشی از یک ریلیز دستی ساخته یا ویرایش نمی‌شود.

## امکانات

- **مدیریت هاست‌ها** — ساخت، فهرست‌کردن، فعال/غیرفعال‌کردن و حذف هاست‌های مجازی، با نام‌گذاری
  بر اساس دامنه (`sites-available/example.com`، نه یک شناسه‌ی گنگ)، و لینک‌شده به
  `sites-enabled` دقیقاً همان‌طور که Nginx انتظار دارد.
- **پراکسی معکوس و هاست‌های استاتیک** — راه‌اندازی مرحله‌به‌مرحله برای host/port بالادستی،
  پروتکل، نسخه‌ی HTTP، تایم‌اوت‌ها، محدودیت حجم بدنه‌ی درخواست و هدرهای forward.
- **Location‌ها** — می‌توانید location‌های اضافیِ پراکسی معکوس به یک هاست اضافه کنید، یا یک
  بلوک location کاملاً سفارشی برای موارد پیشرفته بسازید.
- **پشتیبانی از WebSocket** — فقط یک سوییچ بله/خیر می‌زنید و دایرکتیوهای درست
  `Upgrade`/`Connection`/`proxy_http_version` خودشان تنظیم می‌شوند؛ دیگر لازم نیست خودتان
  بنویسیدشان.
- **SSL/TLS** — گواهی‌ها را از یک دایرکتوری متمرکز
  (`/etc/nginx/certs/<domain>/{fullchain,privkey}.pem`) می‌خواند، نه اینکه در هر هاست کپی
  کند؛ پس تمدید یک گواهی فقط جایگزین‌کردن همان دو فایل است. هم RSA و هم ECDSA پشتیبانی
  می‌شوند و به‌طور خودکار تشخیص داده می‌شوند.
- **بازرسی گواهی** — تاریخ انقضا (هم خوانا برای انسان، هم دقیق)، صادرکننده، subject، SANها،
  اثرانگشت SHA-256، تطابق کلید/گواهی و اعتبارسنجی زنجیره، همه با `crypto/x509` خود Go (بدون
  نیاز به صدازدن `openssl` به‌عنوان زیرفرآیند).
- **Access List (احراز هویت پایه‌ی HTTP)** — فهرست‌های کاربریِ قابل‌استفاده‌ی مجدد و
  bcrypt-hash‌شده در قالب `htpasswd` که می‌توانید روی یک هاست یا یک location خاص اعمالشان کنید.
- **لاگ‌های access/error** — به‌ازای هر هاست جدا فعال/غیرفعال می‌شوند، با logrotate که موقع
  نصب خودش پیکربندی می‌شود.
- **لاگ حسابرسی (audit log)** — هر تغییری که از طریق برنامه انجام شود، با زمان، کاربر لینوکس،
  اکشن، هدف و نتیجه ثبت می‌شود.
- **کنترل و مانیتورینگ سرویس Nginx** — start/stop/restart/reload/test، به‌علاوه‌ی کشف
  master/worker process، مصرف حافظه، uptime و پورت‌های در حال listen، همه مستقیماً از `/proc`
  خوانده می‌شوند.
- **ایمنی اول از همه** — هر تغییر پیکربندی، پشتیبان‌گیری، نوشته و با `nginx -t` تست می‌شود، و
  فقط اگر تست موفق شد reload می‌خورد. اگر تست شکست بخورد، پیکربندی قبلی خودکار برمی‌گردد و
  خطا در audit log ثبت می‌شود. Nginx هیچ‌وقت با پیکربندی خراب رها نمی‌شود.
- **همزیستی با پیکربندی دستی** — هاست‌هایی که GoNix نساخته، به‌صورت read-only و با نشانگر
  واضح `[unmanaged]` نمایش داده می‌شوند، نه اینکه بی‌سروصدا بازنویسی شوند.

## پیش‌نیازها

- لینوکس (از `systemctl`، `/proc` و مسیرهای استاندارد Nginx استفاده می‌کند)
- [Go](https://go.dev) نسخه‌ی ۱.۲۴ یا جدیدتر (اگر می‌خواهید از سورس بسازید)
- Nginx
- `systemctl` (systemd)
- اختیاری: `logrotate` (اگر نباشد، چرخش لاگ فقط با یک هشدار رد می‌شود)
- اختیاری: `certbot`، فقط برای امکان [دریافت گواهی Wildcard (Certbot)](#دریافت-گواهی-wildcard-certbot)
  لازم است (به همراه دامنه‌ای که روی Cloudflare مدیریت می‌شود)

## ساخت از سورس

### نصب از سورس با `make install`

```bash
git clone https://github.com/mrmmg/gonix.git
cd gonix
make install
```

این همان کاری است که نصب‌کننده‌ی یک‌دستوریِ بالا انجام می‌دهد، منتها از سورس — برای وقتی که
نمی‌خواهید باینری از‌پیش‌ساخته را از GitHub Releases بگیرید. این دستور:

۱. باینری را از سورس می‌سازد (با `go build -trimpath -ldflags="-s -w"`).
۲. آن را در `/opt/gonix/bin/gonix` نصب و `/usr/local/bin/gonix` را به آن symlink می‌کند.
۳. پیکربندی پیش‌فرض را در `/etc/gonix/gonix.yaml` می‌گذارد (بدون overwrite کردن نسخه‌ی
   موجود، اگر از قبل داشته باشید).
۴. مسیرهای `/etc/gonix/{backups,accesslists}` و `/var/log/gonix` را می‌سازد.
۵. یک سیاست logrotate در `/etc/logrotate.d/gonix` نصب می‌کند که هم لاگ‌های Nginx هر هاست و
   هم audit log را پوشش می‌دهد (اگر `logrotate` نصب نباشد، فقط یک پیام می‌دهد و رد می‌شود).

بعدش:

```bash
sudo gonix
```

### ساخت توسعه (development build)

```bash
git clone https://github.com/mrmmg/gonix.git
cd gonix

go mod download

# ساخت توسعه
go build -o gonix ./cmd/gonix

# اجرای محلی (root لازم است چون باید /etc/nginx و systemctl را مدیریت کند)
sudo ./gonix
```

### اجرا با `go run` (توسعه)

موقع توسعه می‌توانید مرحله‌ی build صریح را کلاً کنار بگذارید و مستقیم از سورس با `go run`
اجرا کنید. چون `main.go` برای دسترسی به `/etc/nginx` و `systemctl` نیاز به root دارد، آن را
با `sudo` صدا بزنید (اگر می‌خواهید یک `GONIX_CONFIG` سفارشی هم پاس بدهید، از `sudo -E`
استفاده کنید):

```bash
sudo go run ./cmd/gonix
```

چون cache ماژول و cache ساخت معمولاً زیر کاربر خودتان است، اولین اجرای `sudo go run` در یک
چک‌اوت تازه ممکن است مجبور شود یک‌بار همه‌چیز را به‌عنوان root بسازد؛ اگر ترجیح می‌دهید خودِ
`go` را با root اجرا نکنید، به‌جایش `sudo` را روی یک باینری توسعه‌ی از‌پیش‌ساخته اجرا کنید
(`go build -o gonix ./cmd/gonix && sudo ./gonix`).

اگر می‌خواهید به‌جای `/etc/nginx` واقعی از یک پیکربندی موقت استفاده کنید (خوب است برای
امتحان‌کردن رابط کاربری بدون دست‌زدن به تنظیمات واقعی Nginx)، `configs/default.yaml` را کپی
کنید، مسیرهایش را به دایرکتوری‌های موقت محلی تغییر دهید و این‌طور اجرا کنید:

```bash
sudo GONIX_CONFIG=/path/to/dev-gonix.yaml go run ./cmd/gonix
```

### ساخت نسخه‌ی نهایی (production)

```bash
go build -trimpath -ldflags="-s -w" -o gonix ./cmd/gonix
```

- `-trimpath` مسیرهای فایل‌سیستم محلی را از باینری کامپایل‌شده پاک می‌کند، تا build
  تکرارپذیر (reproducible) و بدون افشای اطلاعات محلی داشته باشیم.
- `-ldflags="-s -w"` جدول نمادها (symbol table) و اطلاعات دیباگ DWARF را حذف می‌کند و باینری
  کوچک‌تر می‌شود.

### کراس-کامپایل

```bash
GOOS=linux GOARCH=amd64 go build -trimpath -ldflags="-s -w" -o gonix-linux-amd64 ./cmd/gonix
GOOS=linux GOARCH=arm64 go build -trimpath -ldflags="-s -w" -o gonix-linux-arm64 ./cmd/gonix
```

این دقیقاً همان کاری است که [`.github/workflows/release.yml`](.github/workflows/release.yml)
برای هر معماری انجام می‌دهد (به‌علاوه‌ی پاس‌دادن `-X .../internal/tui.Version=<tag>` تا
`gonix --version` نسخه‌ی منتشرشده را نشان بدهد)؛ برای همین نام فایل‌ها هم یکسان است:
`gonix-linux-amd64` و `gonix-linux-arm64`.

### Makefile

یک `Makefile` هم دستورات بالا را برایتان جمع کرده:

```bash
make build     # ساخت توسعه
make release   # ساخت نهایی (trimpath + stripped)
make cross     # کراس-کامپایل برای linux/amd64 و linux/arm64
make test      # go test ./...
make vet       # go vet ./...
make lint      # بررسی vet + fmt
make install   # ساخت نهایی + نصب در /opt/gonix (در صورت نیاز رمز sudo پرسیده می‌شود)
make clean     # حذف باینری‌های ساخته‌شده
```

## دستورات توسعه

```bash
go test ./...
go vet ./...
go fmt ./...
```

تست‌ها از دایرکتوری‌های موقت و پیاده‌سازی‌های fake برای دستورات خارجی (`nginx -t`،
`systemctl`) استفاده می‌کنند، برای همین کل مجموعه‌ی تست بدون نیاز به نصب واقعی Nginx یا
دسترسی root اجرا می‌شود — هیچ‌کدام از دستورات بالا نیازی به `sudo` ندارند.

چند حالت کاربردی برای وقتی که روی یک پکیج خاص کار می‌کنید:

```bash
go test ./internal/nginx/...           # فقط یک پکیج
go test ./... -run TestRenderHost -v   # فقط یک تست، به‌صورت verbose
go test ./... -cover                   # همراه با خلاصه‌ی پوشش تست (coverage)
```

## ساختار پروژه

```text
gonix/
├── cmd/gonix/             نقطه‌ی ورود: پیکربندی، سرویس‌های backend و TUI را به هم وصل می‌کند
├── internal/
│   ├── tui/               صفحات Bubble Tea، ناوبری و استایل‌دهی — هیچ‌وقت مستقیم به فایل‌ها دست نمی‌زند
│   ├── nginx/             مدل دامنه (Host/Location/...)، رندرکننده‌ی قالب، عملیات فایل‌سیستم
│   │                      (sites-available/enabled)، اعتبارسنج `nginx -t`، و یک
│   │                      پارسر best-effort برای پیکربندی‌های مدیریت‌شده و دستی
│   ├── hosts/             گردش‌کار امن و سطح‌بالا: اعتبارسنجی → پشتیبان‌گیری → نوشتن → تست →
│   │                      reload-یا-rollback → audit
│   ├── certificates/      کشف و بازرسی گواهی TLS (crypto/x509)
│   ├── acme/               دریافت خودکار گواهی wildcard: certbot + Cloudflare API با DNS-01
│   │                       (نگاه کنید به «دریافت گواهی Wildcard (Certbot)» زیر گواهی‌های SSL)
│   ├── accesslist/        «Access List»های احراز هویت پایه‌ی HTTP، بر پایه‌ی فایل‌های htpasswd (bcrypt)
│   ├── audit/             لاگ حسابرسیِ append-only
│   ├── backup/            پشتیبان‌گیری/بازیابی پیکربندی
│   ├── system/             کنترل systemctl + مانیتورینگ process و پورت بر پایه‌ی /proc
│   ├── logs/               کمک‌تابع‌های مسیر لاگ + تولید پیکربندی logrotate
│   └── config/             پیکربندی متمرکز و مبتنی بر YAML (بدون مسیر hard-code شده)
├── templates/             قالب‌های پیکربندی Nginx که embed شده‌اند (host.tmpl، location.tmpl)
├── configs/
│   ├── default.yaml       پیکربندی پیش‌فرض GoNix، که به‌عنوان یک release asset هم منتشر می‌شود
│   └── logrotate.conf     سیاست logrotate که توسط `make install` و install.sh نصب می‌شود
├── install.sh             نصب‌کننده‌ی یک‌دستوری: یک ریلیز را دانلود می‌کند، بدون نیاز به Go toolchain
├── Makefile               دستور `make install` از سورس می‌سازد و همان ساختار را نصب می‌کند
├── .github/workflows/
│   └── release.yml        با هر push شدن تگ vX.Y.Z یک GitHub Release می‌سازد و منتشر می‌کند
└── tests/                 (فایل‌های *_test.go کنار هر پکیج، مجموعه‌ی تست واقعی را تشکیل می‌دهند)
```

مسئولیت‌ها لایه‌بندی شده‌اند: TUI به `internal/hosts` (لایه‌ی گردش‌کار) فراخوانی می‌زند، و آن
هم خودش به `internal/nginx` (رندر + فایل‌سیستم)، `internal/backup` و `internal/audit` فراخوانی
می‌زند. TUI هیچ‌وقت مستقیماً در `/etc/nginx` چیزی نمی‌نویسد.

## پیکربندی Nginx

هاست‌ها دقیقاً به روش استاندارد Nginx ذخیره می‌شوند:

```text
/etc/nginx/sites-available/example.com   # همیشه موجود است، منبع اصلی حقیقت (source of truth)
/etc/nginx/sites-enabled/example.com     # symlink به sites-available، فقط وقتی فعال باشد موجود است
```

غیرفعال‌کردن یک هاست فقط symlink را حذف می‌کند؛ خودِ فایل پیکربندی هیچ‌وقت حذف نمی‌شود. فایل‌های
پیکربندی‌ای که GoNix می‌سازد با یک کامنت `# Managed by GoNix.` شروع می‌شوند، و برنامه از طریق
همین کامنت در اسکن‌های بعدی فایل‌های خودش را از فایل‌های دستی تشخیص می‌دهد. هاست‌های بدون این
نشانگر به‌صورت **[unmanaged]** فهرست و در TUI read-only نمایش داده می‌شوند — همچنان می‌توانید
پیکربندی خامشان را ببینید، ولی ویرایش آن‌ها به عهده‌ی یک ویرایشگر متن است تا چیزی که GoNix
نساخته خراب نشود.

## گواهی‌های SSL

گواهی‌ها باید در یک دایرکتوری متمرکز باشند (قابل‌تنظیم، پیش‌فرض `/etc/nginx/certs`):

```text
/etc/nginx/certs/
├── example.com/
│   ├── fullchain.pem
│   └── privkey.pem
└── example.org/
    ├── fullchain.pem
    └── privkey.pem
```

پیکربندی ساخته‌شده‌ی هاست مستقیماً به همین فایل‌ها ارجاع می‌دهد:

```nginx
ssl_certificate /etc/nginx/certs/example.com/fullchain.pem;
ssl_certificate_key /etc/nginx/certs/example.com/privkey.pem;
```

GoNix گواهی‌ها را در دایرکتوری‌های هر هاست کپی **نمی‌کند**، پس تمدید یک گواهی (مثلاً با
`certbot` و DNS challenge — چه دستی/خارجی اجرا شود، چه با فرایند خودکار زیر) فقط جایگزین‌کردن
همان دو فایل سرجایشان است — بدون هیچ پیکربندی مجدد.

### دریافت گواهی Wildcard (Certbot)

از صفحه‌ی **گواهی‌ها (Certificates)**، اولین گزینه — **«Get Wildcard Certificate (Certbot)»** —
کل فرایند دریافت یک گواهی wildcard (`*.domain.com` + `domain.com`) را با DNS-01 challenge
`certbot` و **Cloudflare API** به‌صورت خودکار انجام می‌دهد. در حال حاضر تنها ارائه‌دهنده‌ی DNS
پشتیبانی‌شده همین Cloudflare است.

به‌طور معمول، دستور `certbot certonly --manual --preferred-challenges dns` نیاز به دخالت انسان
دارد: یک TXT record چاپ می‌کند که باید بسازید، منتظر می‌ماند تا آن را در پنل DNS تنظیم کنید، و
از شما می‌خواهد بعد از اطمینان از انتشار آن (مثلاً با `dig`) کلید Enter را بزنید. GoNix این
بخش تعاملی را با مکانیزم خودِ certbot یعنی `--manual-auth-hook`/`--manual-cleanup-hook` جایگزین
می‌کند — certbot خودش یک دستور hook را اجرا می‌کند و منتظر تمام‌شدنش می‌ماند، پس هیچ تعامل با
ترمینال لازم نیست. این hook همان باینری `gonix` است که به‌شکل `gonix acme-hook auth|cleanup`
فراخوانی می‌شود (یک زیردستور مخفی؛ نگاه کنید به `internal/acme`) و کارهای زیر را انجام می‌دهد:

۱. ساخت رکورد TXT با نام `_acme-challenge.domain.com` از طریق Cloudflare API.
۲. پرس‌وجوی مستقیم از DNS عمومی (بدون کش‌های محلی) تا زمانی که رکورد قابل‌مشاهده شود — همان
   جایگزین مرحله‌ی دستیِ `dig` و زدن Enter.
۳. اجازه‌دادن به certbot برای اعتبارسنجی challenge و صدور گواهی.
۴. حذف رکورد TXT بعد از اتمام کار از طریق Cloudflare API (hook مربوط به cleanup).

این ویزارد سه چیز می‌پرسد:

| فیلد                  | توضیح                                                                    |
|------------------------|---------------------------------------------------------------------------|
| دامنه‌ی پایه           | بدون `*.` در ابتدا، مثلاً `example.com`                                   |
| توکن API کلادفلر       | باید دسترسی `Zone:DNS:Edit` روی زون همان دامنه داشته باشد؛ ورودی مخفی است |
| ایمیل تماس             | اختیاری؛ برای اعلان‌های تمدید Let's Encrypt استفاده می‌شود                |

پیش از اجرای اصلِ `certbot`، GoNix ابتدا توکن Cloudflare را اعتبارسنجی و مطمئن می‌شود دامنه در
همان اکانت Cloudflare زون دارد؛ به این ترتیب یک توکن نامعتبر یا دامنه‌ای که روی Cloudflare نیست،
بلافاصله با یک پیام واضح شکست می‌خورد، نه بعد از شروع اجرای certbot.

در صورت موفقیت، GoNix فایل‌های `fullchain.pem`/`privkey.pem` صادرشده را از مسیر certbot یعنی
`/etc/letsencrypt/live/<domain>/` به دایرکتوری متمرکز گواهی‌ها
(`<certificates.directory>/<domain>/`، همان‌طور که در [گواهی‌های SSL](#گواهیهای-ssl) بالا آمد)
کپی می‌کند و روی آن‌ها `chmod 0644` می‌زند تا Nginx همیشه بتواند بخواندشان — دقیقاً مثل یک گواهی
که دستی جایگذاری شده باشد. این کار چه certbot یک گواهی کاملاً جدید صادر کرده باشد، و چه — طبق
رفتار پیش‌فرضش وقتی گواهی هنوز موعد تمدید نرسیده — همان گواهی قبلی (یک ورودی در
`/etc/letsencrypt/archive/` و symlinkهای `live/` مربوط به آن) را دست‌نخورده رها کرده باشد، درست
کار می‌کند: در هر دو حالت، هر چه الان در `live/` باشد نصب می‌شود.

**پیش‌نیازها:** `certbot` باید از قبل روی سرور نصب باشد (GoNix آن را نصب نمی‌کند)، و DNS دامنه
باید توسط همان اکانت Cloudflareای مدیریت شود که توکن API متعلق به آن است. هر اجرا در
[لاگ حسابرسی](#لاگها) با نام `wildcard_certificate_issued` ثبت می‌شود.

**نمایش زنده‌ی پیشرفت، نه یک صفحه‌ی فریزشده:** بیشترِ این زمان صرف انتظار برای منتشرشدن رکورد TXT
روی DNS عمومی می‌شود — که می‌تواند چند دقیقه طول بکشد. به‌جای اینکه رابط کاربری فریزشده به‌نظر
برسد، GoNix خروجی خودِ certbot را همان لحظه که تولید می‌شود پخش می‌کند، به‌همراه یک پیام دوره‌ای
(heartbeat)، تا همیشه معلوم باشد کاری در حال انجام است؛ لاگ کامل هم بعد از تمام‌شدن اجرا (چه موفق،
چه ناموفق) روی صفحه می‌ماند و با `↑↓` قابل اسکرول است.

#### تمدید یک گواهی

انتخاب یک گواهیِ موجود از صفحه‌ی **گواهی‌ها**، یک زیرمنوی کوچک با دو گزینه‌ی **«View Details»** و
**«Renew Certificate (Certbot + Cloudflare)»** باز می‌کند. تمدید:

- فقط دوباره توکن API کلادفلر را می‌پرسد (و اختیاری، یک ایمیل) — نه دامنه را، که از قبل مشخص است،
  و توکن هیچ‌وقت بین اجراها ذخیره نمی‌شود.
- فلگ `--force-renewal` را به certbot پاس می‌دهد، پس بعد از تأیید بدون قید‌وشرط اجرا می‌شود
  (برخلاف یک اجرای تازه‌ی «Get Wildcard Certificate» که به‌طور پیش‌فرض گواهی هنوز منقضی‌نشده را
  دست‌نخورده رها می‌کند).
- اگر گواهی هنوز بیش از ۳۰ روز اعتبار داشته باشد، اول هشدار می‌دهد — دقیقاً همان آستانه‌ی پیش‌فرض
  خودِ certbot و Let's Encrypt برای تمدید — چون تمدید زودتر از این فقط بدون فایده به rate limit
  حساب Let's Encrypt لطمه می‌زند. همچنان می‌توانید تأیید کنید و به‌هرحال تمدید کنید (مثلاً اگر تازه
  چیزی در تنظیمات DNS دامنه تغییر داده‌اید و می‌خواهید مطمئن شوید فرایند درست کار می‌کند).
- در صورت موفقیت، `nginx -t` را دوباره اجرا و Nginx را خودکار reload می‌کند: مسیر فایل گواهی در
  دایرکتوری متمرکز گواهی‌ها با تمدید تغییر نمی‌کند، پس هر هاستی که از قبل از آن استفاده می‌کند فقط
  نیاز دارد Nginx فایل (حالا به‌روزشده) را دوباره بخواند، نه یک تغییر پیکربندی.

## Access List‌ها

یک Access List مجموعه‌ای نام‌گذاری‌شده و قابل‌استفاده‌ی مجدد از کاربران احراز هویت پایه‌ی HTTP
است که به‌صورت یک فایل استاندارد در قالب `htpasswd` نگهداری می‌شود (رمزهای عبور bcrypt-hash
شده — متن خام هیچ‌وقت ذخیره نمی‌شود)، در `/etc/gonix/accesslists/<name>.htpasswd`. وقتی یک
Access List را روی یک هاست یا location اعمال می‌کنید، این خطوط اضافه می‌شوند:

```nginx
auth_basic "Restricted";
auth_basic_user_file /etc/gonix/accesslists/<name>.htpasswd;
```

> **نکته:** رکوردهای htpasswd با هش bcrypt نیاز دارند که Nginx به یک پیاده‌سازی libc/crypt که
> از bcrypt پشتیبانی می‌کند لینک شده باشد (در glibc مدرن از طریق libxcrypt این‌طور است، و در
> اکثر توزیع‌های لینوکس فعلی هم صادق است). اگر build شما از Nginx نتوانست هش‌های bcrypt را
> تأیید کند، رکوردهای آن Access List را با ابزاری که الگوریتم پشتیبانی‌شده توسط `crypt(3)` شما
> را می‌سازد، دوباره بسازید.

## لاگ‌ها

- **لاگ‌های access/error**: به‌ازای هر هاست، در `/var/log/nginx/<domain>.access.log` و
  `<domain>.error.log`، که مستقل از هم از صفحه‌ی مدیریت هر هاست فعال/غیرفعال می‌شوند.
- **لاگ حسابرسی (audit log)**: `/var/log/gonix/audit.log`، هر خط برای یک اکشن:
  `2026-09-15 10:32:11 | user=root | action=host_created | target=example.com | result=success`
- **چرخش (rotation)**: هر دو با `logrotate` استاندارد چرخانده می‌شوند، که هر دو نصب‌کننده خودشان
  در `/etc/logrotate.d/gonix` پیکربندیش می‌کنند.

## پشتیبان‌گیری (Backups)

قبل از اینکه GoNix هر تغییری روی فایل پیکربندی یک هاست اعمال کند، یک نسخه‌ی پشتیبان با
timestamp در `/etc/gonix/backups/` می‌گذارد. بعد از نوشتن، `nginx -t` را اجرا می‌کند:

- **تست موفق** ← Nginx reload می‌شود و تغییر به‌عنوان موفقیت در audit log ثبت می‌شود.
- **تست ناموفق** ← پیکربندی قبلی خودکار از همان پشتیبانی که لحظاتی پیش گرفته بود برمی‌گردد،
  Nginx **هیچ‌وقت** با پیکربندی خراب reload نمی‌شود، و خطا (همراه با خروجی Nginx) در audit log
  ثبت می‌شود.

پشتیبان‌های قدیمی‌تر از تنظیمات `backup.keep_count` (پیش‌فرض ۲۰ نسخه به‌ازای هر فایل) خودکار
پاک می‌شوند. صفحه‌ی «Backup / Restore» روی هر هاست هم به شما اجازه می‌دهد هر نسخه‌ی نگه‌داشته‌شده
را ببینید و دستی برگردانید، با همان مسیر ایمنِ تست-قبل-از-reload.

## توسعه

مشارکت‌ها باید از قراردادهای استاندارد Go پیروی کنند: `gofmt`، `go vet`، و تست برای هر رفتار
جدید. TUI (`internal/tui`) را از دسترسی مستقیم به فایل‌سیستم/Nginx دور نگه دارید — هر قابلیت
جدیدِ backend باید به پکیج مناسب زیر `internal/*` اضافه شود و از طریق `tui.Deps` در اختیار TUI
قرار بگیرد.

## امکانات آینده

اینا عمداً هنوز پیاده‌سازی نشده‌اند، ولی معماری پروژه برایشان طراحی شده:

- یکپارچه‌سازی خودکار با Let's Encrypt / Certbot و ارائه‌دهندگان DNS challenge (مثل Cloudflare)
- فهرست‌های allow/deny بر اساس IP و محدودسازی نرخ (rate limiting)
- هدرهای امنیتی، فشرده‌سازی و پیش‌تنظیم‌های caching
- یکپارچه‌سازی WAF / ModSecurity
- گروه‌های upstream، توزیع بار (load balancing) و health check
- HTTP/3، OCSP stapling، پیش‌تنظیم‌های HSTS
- حالت تعمیر (maintenance mode) و صفحات خطای سفارشی
- درون‌ریزی/دیف‌گیری پیکربندی‌های دلخواه موجود Nginx
- تاریخچه‌ی پیکربندی مبتنی بر Git و rollback
- پشتیبانی از چند نمونه (instance) Nginx
